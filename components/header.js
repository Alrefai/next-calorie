const Header = () => (
  <div className={`flex justify-between items-baseline mv2 pv2 bb`}>
    <h1 className={`f2 mv0`}>Calories Counter</h1>
    <h6 className={`f5 bg-green black br2 pa1 mv0 self-center`}>Demo</h6>
  </div>
)

export default Header
